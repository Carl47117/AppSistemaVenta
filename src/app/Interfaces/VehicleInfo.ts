export interface VehicleInfo {
  vehicleId?: number;
  contactId?: number;
  carMake?: string;
  carModel?: string;
  carYear?: number;
  vin?: string;
  licensePlate: string;
  currentMileage?: number;
}
